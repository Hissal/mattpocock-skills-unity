---
"mattpocock-skills-unity": minor
---

`prototype` gains its Unity delta: in a Unity repo it calls the `unity` router for where a prototype lives, how it runs and is removed, and whether an HTML one fits. `LOGIC.md` and `UI.md` stay verbatim. Its docs page gains one Unity line.
