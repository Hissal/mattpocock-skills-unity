---
"mattpocock-skills-unity": minor
---

`diagnosing-bugs` gains its Unity delta: in a Unity repo it calls the `unity` router for building a feedback loop for a Unity bug, instrumenting or profiling it, and where its regression test goes. The HITL template stays verbatim. The router's debugging row now names building a feedback loop and profiling, and a new row sends a bug's regression test placement to `unity-testing`. Its docs page gains one Unity line.
