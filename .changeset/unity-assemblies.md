---
"mattpocock-skills-unity": minor
---

Add the `unity-assemblies` skill: which assembly Unity code compiles into and what it can see, the traps that break the Editor or the Player build, the Editor/runtime default, `GUID:` references with a hand-written `.meta` for a new `.asmdef` or `.asmref`, optional-package defines with `versionDefines` and `defineConstraints`, and asmdef changes proposed rather than made. It ships `asmdefs.sh`, which maps every assembly's name, GUID, path and resolved references. The `unity` router gains its rows and `ask-matt` its line.
