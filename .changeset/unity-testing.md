---
"mattpocock-skills-unity": minor
---

Add the `unity-testing` skill: EditMode `[Test]` by default and PlayMode only when the engine must run, the humble MonoBehaviour seam and the engine behind the repo's own port, the EditMode traps (no lifecycle, `DestroyImmediate`, error logs fail tests), statics reset in `[SetUp]` as if domain reload is off, which tests to run and when, and the Unity run cadence (batch independent slices only on cold headless launches, never dropping the red run). It discloses explicit test assembly templates and first-assembly setup, `[UnityTest]` and async tests, and C# examples. The `unity` router gains its rows, `ask-matt` its line, and setup's Unity config the **Testing** fields.
