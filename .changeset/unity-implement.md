---
"mattpocock-skills-unity": minor
---

`implement` gains its Unity delta: in a Unity repo it calls the `unity` router for how often to run the tests and checks, and for checking a change compiles or works before calling it done. When Unity cannot run, it still commits and its final message carries `unity-verification`'s report. Its docs page gains one Unity line.
