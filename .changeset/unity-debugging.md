---
"mattpocock-skills-unity": minor
---

Add the `unity-debugging` skill: the Unity form of each feedback-loop kind; a tested Play-loop script that enters Play, waits for the scenario, greps the console since a cursor and exits red, green or no verdict (stepping frames when the editor is unfocused, and running twice from a fresh domain reload when domain reload is off to flag second-Play bugs); `eval` and `run_script` as the REPL, with an IDE debugger as a human step; a bug-class table that calls the sibling Unity skills; `Debug-<tag>` repro folders under the prototype folder; Development Build and `-logFile` rules for players; and baseline-first performance measurement. `unity-prototyping` now calls it for driving Play mode; the `unity` router gains its row and `ask-matt` its line.
